tracer.property: "I AM THE DEFAULT FROM GIT"

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/users
    username: postgres
    password: 123456789
    testWhileIdle: true
    validationQuery: "SELECT 1"